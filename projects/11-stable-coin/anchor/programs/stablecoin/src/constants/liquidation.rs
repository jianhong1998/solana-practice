pub const LIQUIDATION_THRESHOLD: u64 = 50;

/**
 * Percentage of incentive for liquidate an account
*/
pub const LIQUIDATION_BONUS: u64 = 10;

/**
 * Account can be liquidated if the account health factor fall below this value
*/
pub const MIN_HEALTH_FACTOR: u64 = 1;
